{
  "name": "xcode-skills",
  "version": "0.1.4",
  "description": "Manage Agent Skills for Xcode's Codex and Claude Coding Assistant integrations.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/darsha80/xcode-skills.git"
  },
  "type": "module",
  "license": "MIT",
  "bin": {
    "xcode-skills": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prepublishOnly": "npm test && npm run typecheck && npm run build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "skills": "^1.5.9"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vitest": "^4.0.0"
  }
}
